namespace RizzyUI;

/// <summary>
/// Specifies where the color preview thumbnail is rendered relative to the text input.
/// </summary>
public enum ColorPickerThumbnailPosition
{
    /// <summary>
    /// Renders the thumbnail before the input.
    /// </summary>
    Start,

    /// <summary>
    /// Renders the thumbnail after the input.
    /// </summary>
    End
}
