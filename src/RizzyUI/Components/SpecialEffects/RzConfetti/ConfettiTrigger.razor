@namespace RizzyUI
@inherits RzAsChildComponent<ConfettiTrigger.Slots>

@if (AsChild)
{
    @RenderAsChild()
}
else
{
    <HtmlElement Element="@EffectiveElement"
                 @attributes="@GetComponentAttributes()">
        @ChildContent
    </HtmlElement>
}
