
@page "/test"
@using Rizzy.Components
@using Rizzy
@using RizzyUI.Docs.Models
@using RizzyUI.Docs.Components.Pages.Components.Demo

<div class="mx-auto p-8 mb-5 flex w-full max-w-sm flex-col items-center justify-center gap-6">
    <EditForm Model="_demoModel">
        <RzTabs DefaultValue="account">
            <TabsList>
                <TabsTrigger Value="account">Account</TabsTrigger>
                <TabsTrigger Value="password">Password</TabsTrigger>
            </TabsList>
            <TabsContent Value="account">
                <RzCard>
                    <CardHeader>
                        <CardTitle>Account</CardTitle>
                        <CardDescription>
	                        Make changes to your account here. Click save when you're done.
                        </CardDescription>
                    </CardHeader>
                    <CardContent class="grid gap-6">
                        <Field>
	                        <FieldLabel For="@(() => _demoModel.Name)">Name</FieldLabel>
	                        <RzInputText For="@(() => _demoModel.Name)" />
                        </Field>
                        <Field>
	                        <FieldLabel For="@(() => _demoModel.Username)">Username</FieldLabel>
	                        <RzInputText For="@(() => _demoModel.Username)" />
                        </Field>
                    </CardContent>
                    <CardFooter>
                        <RzButton>Save changes</RzButton>
                    </CardFooter>
                </RzCard>
            </TabsContent>
            <TabsContent Value="password">
                <RzCard>
                    <CardHeader>
                        <CardTitle>Password</CardTitle>
                        <CardDescription>
	                        Change your password here. After saving, you'll be logged out.
                        </CardDescription>
                    </CardHeader>
                    <CardContent class="grid gap-6">
                        <Field>
	                        <FieldLabel For="@(() => _demoModel.CurrentPassword)">Current password</FieldLabel>
	                        <RzInputText For="@(() => _demoModel.CurrentPassword)" Role="TextRole.Password" />
                        </Field>
                        <Field>
	                        <FieldLabel For="@(() => _demoModel.NewPassword)">New password</FieldLabel>
	                        <RzInputText For="@(() => _demoModel.NewPassword)" Role="TextRole.Password" />
                        </Field>
                    </CardContent>
                    <CardFooter>
                        <RzButton>Save password</RzButton>
                    </CardFooter>
                </RzCard>
            </TabsContent>
        </RzTabs>
    </EditForm>
</div>

<div class="mx-auto mb-5 flex flex-wrap items-center justify-center gap-4 p-8">

	@foreach (var user in _users)
	{
		<UserCard User="user" />
	}
</div>

@code {
	private List<UserData> _users = new()
	{
		new() { Id = 1, Name = "Alice", Email = "alice@example.com" },
		new() { Id = 2, Name = "Bob", Email = "bob@example.com" },
		new() { Id = 3, Name = "Charlie", Email = "charlie@example.com" }
	};
	
	private class TabsDemoModel
	{
		public string Name { get; set; } = "Pedro Duarte";
		public string Username { get; set; } = "@peduarte";
		public string CurrentPassword { get; set; } = string.Empty;
		public string NewPassword { get; set; } = string.Empty;
	}

	private TabsDemoModel _demoModel = new();	
}